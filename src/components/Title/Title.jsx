import React from 'react'
import { TitleComponent } from './title-style'

const Title = ({ title }) => {
  return (
    <TitleComponent>
      { title }
    </TitleComponent>
  )
}

export default Title;
